name: "Docker Image Push"
description: "Pushes the docker image to Docker Hub"
inputs:
  docker_username:
    description: "Docker username"
    required: true
  docker_password:
    description: "Docker password"
    required: true
  image_name:
    description: "Full name of the Docker image (with tag)"
    required: true

runs:
  using: composite
  steps:
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
    - name: Tag Docker Image
      shell: bash
      run: |
        docker tag ${{inputs.image_name}} ${{ secrets.DOCKERHUB_USERNAME }}/${{inputs.image_name}}
    - name: Push Docker Image
      shell: bash
      run: |
        # Push the tagged image to Docker Hub
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{inputs.image_name}}
